---

---

<nav class="fixed bottom-4 px-4 z-10">
  <ul class="flex gap-x-8">
    <!-- {
      menuItems.map((item) => (
        <li class="h-fit">
          <a class="link-primary para-lg" href={item.url}>
            {item.title}
          </a>
        </li>
      ))
    } -->
  </ul>
</nav>
